import { useSearchParams } from 'react-router-dom'

export function ViewLaporanJournal() {
  const [searchParams] = useSearchParams()

  return <div className="px-4 bg-white">ViewLaporanJournal</div>
}
